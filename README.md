A Random Password Creator that can either generate a certain amount of passwords, or one passwords depending on the API you use
Issues:
  1. Passwords are generated by the Random function in java so a skilled hacker could reverse engenerr a password
     The fix is to generate passwords with a set seed that would be inputed by the player
